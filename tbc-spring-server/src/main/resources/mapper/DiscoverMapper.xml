<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTDMapper3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.team2.tbcserver.mapper.DiscoverMapper">
	
	<select id="projectList" resultType="com.team2.tbcserver.vo.ProjectVO">
		SELECT	id,
				creatorId,
				longTitle,
				shortTitle,
				mainImg,
				content,
				category,
				url,
				tags,
				fundingGoalAmount,
				favoriteCount,
				fundingStart,
				fundingEnd,
				rewards,
				favoriteCount,
				fundedAmount,
				sponsorCount,
				createdDate,
				updatedDate
		FROM 	project
	   ORDER BY fundedAmount DESC
	</select>
	
	<select id="remainDate" parameterType="Long" resultType="Long">
	SELECT DATEDiff(fundingEnd, now())
	FROM project
	</select>
	
	<select id="countProject" parameterType="Long" resultType="Long">
	SELECT COUNT(id)
	FROM project
	</select>

</mapper>